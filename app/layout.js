// "use client";

import "./globals.css";
import { Inter } from "next/font/google";
import IsNotLogin from "./components/navbar/isNotLogin/isNotLogin";
// import IsLogin from "./components/navbar/isLogin/isLogin";
// import { Suspense } from "react";

const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  // console.log(location.hash);
  // const idUser = typeof window !== "undefined" ? localStorage.idUser : null;

  return (
    <html lang="en">
      <body className={inter.className}>
        {/* <Suspense fallback={<div>Hey hey, its loading</div>}> */}
        {/* <div className="">{idUser ? <IsLogin /> : <IsNotLogin />}</div> */}
        <IsNotLogin />
        {/* </Suspense> */}
        {/* <div>
            <div>{idUser ? <IsLogin /> : <div />}</div>
            <div>{!idUser ? <IsNotLogin /> : <div />}</div>
          </div> */}
        {/* <Suspense fallback={<Loading />}>{children}</Suspense> */}
        {/* this happens automatically */}
        {children}
        {/* //Es el recibido y renderizado - dynamic - */}
      </body>
    </html>
  );
}
